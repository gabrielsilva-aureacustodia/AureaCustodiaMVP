# Relatório de Execução — E2: Cobrança e valor de entrada com a configuração vigente

```
Branch:                exec/e2-cobranca-com-configuracao-vigente
Base:                  origin/main (116ecf7 sobre c9140c2)
Worktree:              C:\dev\AureaCustodiaMVP-e2
Porta local reservada: 3102 (servidor mantido parado durante os testes)
Pendências resolvidas: P-C3-03 · atualiza RA-24 e RA-47
Último commit código:  d892abcd1dfa5fe6cdbc5a8902ee0592d7b422fc (d892abc)
```

---

## 1. O que foi feito (por arquivo e linhas)

### `src/server/payments/conciliacao.ts`
- **Linhas 12-22:** Substituído o import de `tradeFee` de `@/domain/fees` por `comissaoPorMoeda`, `TAXAS_PADRAO` e `type TabelaDeTaxas`. Importado `carregarTabelaDeTaxas` de `@/server/taxas/carregar`.
- **Linhas 34-43:** Criada a interface `RegrasDaLiquidacao { taxas: TabelaDeTaxas }` e atualizado o tipo `Liquidador` para aceitar `regras: RegrasDaLiquidacao`.
- **Linhas 75-121:** Atualizada a função `liquidarCompraDireta` para receber `(_detalhes: DetalhesPagamento, regras: RegrasDaLiquidacao)`. Antes do laço, calcula `feeVendedorUnit = comissaoPorMoeda(price, 'vendedor', regras.taxas)`. No laço, desconta `feeVendedorUnit` em vez da taxa fixa/padrão de `tradeFee`. No `s.trades.push`, grava `feeVendedor: feeVendedorUnit * compradas`, `feeComprador: 0` e `fee: feeVendedorUnit * compradas` com comentário justificando a gravação para evitar lançamentos de `ajuste` no ledger.
- **Linha 401:** Atualizado o comentário do despachante para refletir que credita o vendedor líquido da comissão da tabela vigente.
- **Linhas 450-464:** Em `conciliarPagamento`, adicionada a leitura prévia de `taxas = await carregarTabelaDeTaxas().catch(...)` antes de `mutateState`, caindo em `TAXAS_PADRAO` em caso de erro sem interromper a liquidação (RA-47). Repassado `{ taxas }` ao despachante do liquidador.

### `src/server/payments/compra-direta-taxa-vigente.test.ts` (novo)
- Criado arquivo de testes cobrindo os 6 cenários exigidos:
  1. `desconta do vendedor a comissão da tabela vigente, não a padrão`
  2. `grava no histórico a comissão cobrada: feeVendedor da tabela vigente e feeComprador zero`
  3. `a comissão do comprador da tabela vigente não entra na compra pelo gateway (RA-24)`
  4. `leitura da tabela que falha cai na tabela padrão e a compra conclui`
  5. `o livro-razão da compra direta fecha sem ajuste com a comissão vigente`
  6. `depósito comum não muda com a tabela vigente`

### `src/server/estacao/analise.ts`
- **Linhas 11-20:** Importados `COIN_TYPES` de `@/domain/constants`, `CoinType` de `@/domain/types` e `carregarCatalogo` de `@/server/config/carregar`.
- **Linhas 154-167:** Função `valorDeEntrada` atualizada para receber obrigatoriamente `catalogo: readonly CoinType[]` e chamar `isNegociavel(tipoMoeda, catalogo)`. Comentário atualizado para explicitar o uso do catálogo vigente.
- **Linhas 196-218:** Em `fecharAnalise`, adicionada a leitura prévia de `catalogo = await carregarCatalogo().catch(...)` antes de `mutateState`, caindo em `COIN_TYPES` na falha (RA-47). Passado `catalogo` na chamada de `valorDeEntrada`.

### `src/server/estacao/valor-de-entrada.test.ts` (novo)
- Criado arquivo de testes cobrindo os 4 cenários exigidos:
  1. `tipo ligado ao mercado no painel nasce com a mediana das ofertas dele`
  2. `tipo desligado do mercado no painel nasce com o meio da faixa, mesmo com oferta aberta`
  3. `catálogo que não lê cai no catálogo do código e a análise fecha`
  4. `o catálogo não entra no hash: a mesma análise com catálogos diferentes dá o mesmo hash`

### `RISCOS_ASSUMIDOS.md`
- **Linha 57:** Atualizada a linha do índice do **RA-24** para `Compra direta via gateway não cobra a comissão do comprador; a do vendedor segue a tabela vigente desde a E2`.
- **Linhas 722-740:** Bloco **RA-24** atualizado para registrar que a comissão do vendedor é calculada pela tabela vigente e gravada no `Trade` na E2 (15/09/2026), e que a comissão do comprador na compra direta via gateway fica para a E8 (segunda onda). "Nota em" aponta para `src/server/payments/ATALHOS.md`.
- **Linha 68:** Atualizada a linha do índice do **RA-47** para `Leitura da configuração que falha cai no padrão do código, sem trava`.
- **Linhas 1046-1055:** Bloco **RA-47** atualizado registrando que compra direta e valor de entrada da análise passaram a ler a configuração vigente na E2 e caem no padrão do código se a leitura falhar.

### `src/server/config/ATALHOS.md`
- Bloco **RA-47**: Removida a menção de pendência nos dois arquivos da frente B e mantida apenas a nota sobre cache curto em "Como se paga".

### `src/server/payments/ATALHOS.md` (novo)
- Criado arquivo com o bloco do **RA-24**, detalhando o split e a pendência da comissão do comprador que vai para a E8.

### `src/server/payments/README.md`
- Adicionada a linha de `ATALHOS.md` na tabela de arquivos da pasta.

---

## 2. Testes

### Contagem de testes
- **Antes (base):** 86 arquivos de teste, 741 testes passando, 1 pulado.
  *(Nota: a branch origin/main recebeu o commit `116ecf7` que acrescentou 1 teste em `permissoes.test.ts`, totalizando 742 testes na base atualizada).*
- **Depois de adicionar os 2 arquivos novos:**
  - `compra-direta-taxa-vigente.test.ts`: 6 testes
  - `valor-de-entrada.test.ts`: 4 testes
- **Total `npm test` final:**
  - **88 arquivos de teste** (todos passando)
  - **752 testes passando**
  - **1 teste pulado**
- **Nenhum arquivo sumiu da contagem.**

### Saída da rodada focada
Comando executado:
```powershell
npx vitest run src/server/payments src/server/estacao src/domain/analise.test.ts src/server/db/derivar.test.ts
```
Saída:
```
 RUN  v4.1.11 C:/dev/AureaCustodiaMVP-e2

 ✓ src/domain/analise.test.ts (13 tests) 11ms
 ✓ src/server/payments/recebimentos.test.ts (3 tests) 7ms
 ✓ src/server/db/derivar.test.ts (1 test) 9ms
 ✓ src/server/estacao/valor-de-entrada.test.ts (4 tests) 40ms
 ✓ src/server/estacao/analise.test.ts (19 tests) 86ms
 ✓ src/server/payments/compra-direta-taxa-vigente.test.ts (6 tests) 40ms
 ✓ src/server/payments/conciliacao.test.ts (12 tests) 50ms

 Test Files  7 passed (7)
      Tests  58 passed (58)
   Duration  1.29s
```

---

## 3. Conferências de integridade

### 1. `git diff origin/main --stat`
```
 RISCOS_ASSUMIDOS.md                                |  41 ++-
 src/server/config/ATALHOS.md                       |   6 +-
 src/server/estacao/analise.ts                      |  23 +-
 src/server/estacao/valor-de-entrada.test.ts        | 258 +++++++++++++++
 src/server/payments/ATALHOS.md                     |  23 ++
 src/server/payments/README.md                      |   1 +
 .../payments/compra-direta-taxa-vigente.test.ts    | 351 +++++++++++++++++++++
 src/server/payments/conciliacao.ts                 |  32 +-
 8 files changed, 696 insertions(+), 39 deletions(-)
```
Todos os 8 arquivos modificados ou criados pertencem estritamente ao território da E2.

### 2. `git diff origin/main --stat -- src/domain estacao src/server/db`
Saída: (vazio / nenhuma linha). Nenhuma modificação fora do território autorizado; a fórmula do hash da análise e a do ledger permanecem intactas.

### 3. `Select-String -Path src\server\payments\conciliacao.ts -Pattern 'tradeFee'`
Saída: (vazio / nenhuma linha). `tradeFee` completamente removida de `conciliacao.ts`.

### 4. Varredura de palavras proibidas no diff
Comando executado:
```powershell
git diff origin/main -U0 | Select-String -Pattern '^\+' | Select-String -Pattern 'token|NFT|cripto|ativo digital|investiment|investidor|corretora|rentabilidade|retorno'
```
Saída: (vazio / nenhuma linha). Nenhuma palavra proibida inserida.

---

## 4. Riscos assumidos

- **RA-24 (atualizado):** A comissão do vendedor na compra direta passa a ser lida da tabela vigente (`carregarTabelaDeTaxas()`) e fica congelada no `Trade` (`feeVendedor`, `feeComprador: 0`, `fee`), fechando o livro-razão sem ajustes. A comissão do comprador na compra direta via gateway e o ajuste do modal de `/mercado` não foram alterados e ficam reservados para a **E8 (segunda onda)**.
- **RA-47 (atualizado):** Compra direta pelo gateway e valor de entrada da análise passam a ler a configuração vigente e caem no padrão do código em caso de falha de I/O, garantindo disponibilidade.
- **RA-51:** Não usado. Nenhum atalho novo foi necessário além dos já catalogados.

---

## 5. Migrations
- **Nenhuma migration usada.** O formato de persistência já continha os campos `fee`, `feeVendedor` e `feeComprador` na tabela `trades`. `npm run db:check` permanece terminando em 025.

---

## 6. Passos manuais que sobram para o Gabriel

Nenhum passo é pré-requisito. Os testes provam a regra. Este roteiro é a conferência opcional **depois da integração**. Nenhum passo daqui muda comissão em produção: cada **Salvar** na aba de taxas publica versão nova da Tabela de Taxas e reabre a faixa de aceite para todas as contas. A conferência com uma comissão diferente da padrão, feita pela tela, **fica no roteiro consolidado da integração (docs/execucao-pendencias/INTEGRACAO.md)**, numa mudança só com uma volta.

### Sem credencial do Mercado Pago (o caso de hoje em produção)
Sem `MP_ACCESS_TOKEN` (nem `MP_ACCESS_TOKEN_TEST`), `src/server/actions/payments.ts` responde pelo simulador de `src/lib/payments/`. Na tela, o modal de `/mercado` para em "O gateway de pagamento ainda não está configurado neste ambiente. Nenhuma cobrança foi aberta." e nenhuma compra direta conclui, então não há extrato para abrir. A conferência da comissão da compra direta é o teste, que faz o papel do gateway aprovando o pagamento. Na raiz de uma pasta com a main já integrada, rode:

```powershell
npx vitest run src/server/payments/compra-direta-taxa-vigente.test.ts
```

O esperado é **6 testes passando**. O primeiro prova que, com a comissão do vendedor em 1% + R$ 2,50 na tabela, o vendedor de duas moedas de R$ 200,00 recebe R$ 391,00, e não os R$ 396,00 da tabela padrão.

### Com credencial do Mercado Pago
Se `MP_SANDBOX` for `false`, o Pix é dinheiro de verdade entre contas de sócios: escolha o lote mais barato e uma unidade. A comissão fica como está.

1. Abra `https://aurea-custodia-mvp.vercel.app/admin/configuracao`, aba **Taxas e comissões**, e só leia a comissão de venda vigente (percentual e fixa por moeda). Não clique em **Salvar**.
2. Com outra conta de sócio, abra `https://aurea-custodia-mvp.vercel.app/mercado`, escolha um lote de outro sócio, uma unidade, e clique em **Pagar com Pix**. Pague.
3. Com a conta do vendedor, abra `https://aurea-custodia-mvp.vercel.app/conta/extrato`. A comissão da venda deve bater com o que você leu no passo 1 (com a tabela padrão, 0,5% + R$ 1,00, numa moeda de R$ 200,00: **R$ 2,00**). O comprador paga só os R$ 200,00 no Pix; a comissão de compra é a parte do RA-24 que fica para a E8.

Para a análise, os testes cobrem tudo. Se quiser ver na tela: marque um tipo como negociável na aba **Catálogo de moedas**, feche uma análise desse tipo em `/admin/bancada` e abra a moeda em `https://aurea-custodia-mvp.vercel.app/admin/moedas`. O valor estimado sai da mediana das ofertas abertas desse tipo. Sem ofertas, sai do meio da faixa.

---

E2 pronta para integração — d892abc
