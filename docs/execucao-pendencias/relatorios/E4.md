# E4 · Relatório — preço da custódia na tela e recibo bloqueado por pendência

Branch `exec/e4-custodia-preco-e-inadimplencia`, base `c9140c2`.

## O que foi feito

**A-4 (preço e textos da custódia)**
- `src/domain/custodia-texto.ts` (novo): `descricaoDaFaturaDeCustodia` (plano anual, plano mensal,
  contratação sem plano, renovação anual, ciclo mensal com o texto de antes) e `custodiaDoEnvio`
  (quantidade do protocolo; valor e período do plano).
- `src/domain/statement.ts`: a linha da fatura usa a descrição por origem; comentários corrigidos.
- `src/app/(app)/envios/page.tsx`: passo 5 usa `custodiaDoEnvio`; `custodiaMensalPorMoeda(quantidade` saiu.
- `src/app/(app)/conta/extrato/page.tsx`: as duas notas dizem a verdade (comissão dos dois lados pela
  Tabela de Taxas, sem número; fatura paga com saldo sai do saldo).
- Comentários velhos em `src/server/actions/custody.ts` (`advanceAnalysis`) e `src/domain/README.md`.

**B-2 (bloqueio por pendência)**
- `src/domain/bloqueio-por-debito.ts` (novo): pendência = marca manual **ou** fatura vencida, calculada
  na hora; `casarOrdensRespeitandoPendencia` tira do casamento as ofertas pausadas e as devolve intactas.
- `src/server/custodia/isencao-da-equipe.ts` (novo): conta que `carregarMembro` reconhece é isenta;
  checagem que falha libera. Perguntado antes do `mutateState`.
- Server Actions: `solicitarRetirada`, `pagarRetiradaComSaldo` (também recusa recibo `'Bloqueado'`),
  `iniciarPixRetirada`, `iniciarCartaoRetirada`, `publishOffer`, `editLot` (aumento), `sellToBid`
  recusam; `buyLot` e `iniciarCompraDireta` recusam lote de vendedor pausado; `publishBid`, `editBid`,
  `publishOffer` e `editLot` casam respeitando a pausa. `bloquearReciboPorDebito` e `desbloquearRecibo`
  exigem `usuarios.editar`. `sellToBid` mostra `apelidoComprador`.
- `src/server/actions/bloqueio-por-debito.ts` (nova Server Action só leitura) e
  `src/components/custody/useBloqueioPorPendencia.ts` (hook que consulta ao abrir a tela).
- Telas: `Certificate.tsx`, `FaturasCustodia.tsx`, `mercado/page.tsx`, `vender/page.tsx`, `MinhasOfertas.tsx`.

**Já estava resolvido:** `custodyCharges` saiu no commit `2cc7194` (migration 013); CD-11 já retirado;
"faixa" só em comentário nas telas do cliente (conforme o documento da E4).

## Testes

- Base: 86 arquivos, 741 passando, 1 pulado. Fim: **91 arquivos, 793 passando, 1 pulado** (52 novos).
- Novos: `custodia-texto.test.ts` (10), `bloqueio-por-debito.test.ts` do domínio (15),
  `statement-custodia.test.ts` (4), `src/server/actions/bloqueio-por-debito.test.ts` (18: recusas,
  liberados, equipe isenta, checagem que falha libera, permissão de bloquear/desbloquear, apelido),
  `useBloqueioPorPendencia.test.ts` (5).
- `retirada.test.ts`, `retirada-ciclo-completo.test.ts`, `ordens.test.ts`, `compra-direta.test.ts` e
  `statement.test.ts` passam sem mudar asserção.
- `npm run typecheck`, `npm run lint`, `npm test`, `npm run build`: verdes.
- `git diff --name-only c9140c2...HEAD` não lista nenhum caminho do "Não pode editar"; `STORE_KEY` não muda.

## Riscos

- **RA-52** — bloqueio calculado na hora, sem gravar no recibo; conta da equipe isenta.
- **RA-53** — a conciliação do gateway não reconfere a pendência (dono: E8, segunda onda).
- **Migration 027 não usada.**

## Achados para a integração

- A marca manual de inadimplência é apagada quando a conta paga qualquer fatura ou quando o ciclo roda,
  e a ficha do painel mostra a marca por fatura como "marca manual" (`src/domain/admin/usuarios.ts`).
- Marcar A-4 e B-2 como feitas nos arquivos de pendências manuais fica para a integração.

## Passos manuais

Nenhum obrigatório: sem migration, sem variável de ambiente, sem painel externo. Roteiro opcional depois
do deploy, com a conta do Gabriel (`gabriel.silva@aureacustodia.com.br`):
1. `/conta/extrato`: as notas não falam em "retida do vendedor" nem em "não é debitada".
2. Com uma conta de teste **fora da equipe**: em `/admin/usuarios/<e-mail da conta de teste>`, seção
   Inadimplência, **Marcar como inadimplente**; entrar com a conta de teste e abrir `/recibos/<código>`
   (carimbo "PENDÊNCIA DE CUSTÓDIA", botões apagados) e `/vender` (aviso). Retirar a marca libera.

E4 pronta para integração — 979fd0e
