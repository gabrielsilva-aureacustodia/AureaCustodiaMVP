# Relatório de Execução — E3 · Limpeza do painel antigo de relatórios

Branch: `exec/e3-limpeza-relatorios-antigos`  
Base: `c9140c2` (`origin/main`)  
Data: 15/09/2026  

---

## 1. O que foi feito

### Arquivos apagados
- `src/components/relatorios/RelatoriosPainel.tsx` (690 linhas): painel antigo de oito abas da rota `/relatorios`, sem uso desde a migração para a Central de Resultados do painel administrativo.
- `src/components/relatorios/README.md` (53 linhas): documentação da pasta removida.
- `src/server/actions/contabil.ts` (236 linhas): ações de escrita contábil sem validação de papel e sem trilha com prefixo `admin.`. Todas as escritas agora passam exclusivamente por `src/server/actions/admin/contabil.ts`.
- **Total:** 3 arquivos e 979 linhas removidos.

### Funções removidas
- `autorizarRelatorio` (síncrona) em `src/server/relatorios/acesso.ts`: removida por conferir apenas lista de ambiente e ignorar os papéis do painel.
- As cinco Server Actions antigas sem conferência de papel: `registrarLancamentoManual`, `estornarLancamentoManual`, `definirParametroContabil`, `sincronizarGoogleSheets` e `verificarIntegridadeLedger`.

### Funções mantidas intactas em `src/server/relatorios/acesso.ts`
- `ehAdmin`: bootstrap síncrono pelo ambiente.
- `tokenDeIntegracaoValido`: comparação em tempo constante da chave de integração.
- `type Autorizacao`: tipo discriminado de resposta.
- `autorizarRelatorioNoPainel`: verificação de sessão com papéis (`resultados.ver` / `resultados.exportar`) e chave de integração de contingência.

### Comentários e documentação atualizados
- `src/domain/admin/contabil.ts`: comentário inicial atualizado registrando a origem das regras e a consolidação do painel como único caminho contábil.
- `src/server/relatorios/acesso.ts`: comentário de `autorizarRelatorioNoPainel` reescrito para explicar a prioridade de sessão sobre a chave de integração e a lista de rotas consumidoras.
- `src/server/relatorios/acesso.test.ts`: cabeçalho atualizado, dublê de `carregarMembro` adicionado e testes reorganizados.
- `src/components/README.md`: linha de `relatorios/` removida da tabela.
- `src/components/admin/README.md`: seção "O painel antigo" reescrita registrando a remoção na E3.
- `src/server/actions/README.md`: linha `contabil.ts` removida da tabela de ações.
- `src/server/actions/admin/README.md`: linha `src/server/actions/contabil.ts` removida de conexões.
- `src/server/actions/ATALHOS.md`: seção RA-16.c atualizada como paga por remoção na E3.
- `src/server/relatorios/README.md`: exportações de `acesso.ts` e contagem de testes atualizadas; linha de `contabil.ts` removida.
- `src/server/relatorios/ATALHOS.md`: `contabil.ts` removido do RA-16.c e nota sobre remoção incluída.
- `src/server/db/README.md`: referência de dependência trocada para `src/server/admin/contabil.ts`.
- `src/app/api/relatorios/README.md`: consumidor interno atualizado para os componentes de `admin/resultados` e nota sobre preservação do contrato JSON.
- `docs/ARQUITETURA_E_PASTAS.md`: linha `src/components/relatorios/` removida do índice.
- `RISCOS_ASSUMIDOS.md`: RA-16 atualizado na tabela resumo (linha 49), no detalhe do item c (linha 552) e adição da nota de atualização de 15/09/2026 (E3).

---

## 2. Testes

### Contagem antes e depois
- **Linha de base (origin/main):** 86 arquivos de teste, 741 testes passando, 1 pulado.
- **Após alterações da E3:** 86 arquivos de teste, 739 testes passando, 1 pulado.
- **Variação líquida:** -2 testes (saíram os 4 testes da antiga função síncrona em `acesso.test.ts` e entraram 2 novos testes).

### Casos novos adicionados (`src/server/relatorios/acesso.test.ts`)
1. `o módulo exporta só a lista do ambiente, a chave de integração e a decisão do painel`
   - Prova que o módulo exporta estritamente `autorizarRelatorioNoPainel`, `ehAdmin` e `tokenDeIntegracaoValido`, garantindo que a versão síncrona antiga não reapareça.
2. `quem entra pela sessão com a permissão continua entrando mesmo com chave errada na URL`
   - Prova que uma sessão autenticada com permissão `resultados.ver` tem precedência sobre uma chave de integração incorreta fornecida no parâmetro da URL.

### Saída resumida do `npm test`
```
Test Files  86 passed (86)
     Tests  739 passed | 1 skipped (740)
  Duration  29.65s
```

---

## 3. O que foi conferido e como

1. **Buscas por referências residuais (vazias):**
   - `git grep -nE 'RelatoriosPainel|@/server/actions/contabil|autorizarRelatorio([^N]|$)' -- src` -> 0 resultados (exit code 1).
   - `git grep -n "components/relatorios" -- src docs/ARQUITETURA_E_PASTAS.md` -> 0 resultados (exit code 1).
2. **Remoção dos arquivos no disco:**
   - `Test-Path src/components/relatorios, src/server/actions/contabil.ts` -> Devolveu `False` para ambos.
3. **Presença das rotas no build de produção (`next build`):**
   - `/relatorios` (redirecionamento dinâmico preservado)
   - `/api/relatorios`
   - `/api/relatorios/[relatorio]`
   - `/api/relatorios/sheets`
   - `/painel`
   - `/admin`
4. **Inspeção de diffs cirúrgicos:**
   - `git diff src/domain/admin/contabil.ts`: apenas linhas de comentário alteradas.
   - `git diff RISCOS_ASSUMIDOS.md`: alterações restritas exclusivamente ao escopo do RA-16 (linha 49 e bloco 552–566).
5. **Verificação de qualidade:**
   - `npm run typecheck` passou sem erros.
   - `npm run lint` passou sem erros.
   - `npm test` passou com todos os 86 arquivos verdes.
   - `npm run build` concluiu com sucesso.

---

## 4. Riscos

- **Consumidor interno da API JSON:** Com a remoção de `RelatoriosPainel.tsx`, as rotas `/api/relatorios/*` no formato JSON ficam sem consumidor interno na interface web, mas permanecem em funcionamento e intactas cumprindo o contrato público registrado em `docs/API_RELATORIOS.md`.
- **Registros históricos no banco de auditoria:** As linhas existentes gravadas com códigos contábeis antigos (`contabil.*`, `ledger.verificacao`) em `audit_log` permanecem no banco para preservação de histórico e conformidade.

---

## 5. Passos manuais que sobram para o Gabriel

Nenhum. A limpeza é totalmente interna de código e documentação. Não há migrations, variáveis de ambiente novas nem configurações externas a aplicar.

---

E3 pronta para integração — 6812652
