# `/admin/cs` — provisória, entra na C2

`page.tsx` confere `cs.ver` e mostra o que a área vai trazer (`AreaEmConstrucao`). A C2 substitui
este arquivo pela caixa de conversas com a ficha do cliente (plano do Admin, seção 2.5).
