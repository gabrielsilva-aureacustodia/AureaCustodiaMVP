# `/admin/moedas` — provisória, entra na C3

`page.tsx` confere `bancada.auditoria` e mostra o que a área vai trazer. A C3 substitui este
arquivo pela auditoria do acervo com a verificação da corrente (plano do Admin, seção 3.5).
