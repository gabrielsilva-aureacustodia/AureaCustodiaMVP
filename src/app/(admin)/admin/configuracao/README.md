# `/admin/configuracao` — provisória, entra na C3

`page.tsx` confere `config.ver` e mostra o que a área vai trazer. A C3 substitui este arquivo pela
configuração de taxas, catálogo, parâmetros e integrações (plano do Admin, seção 3.3).
